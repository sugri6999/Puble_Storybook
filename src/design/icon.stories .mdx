import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import * as Icons from "@storybook/icons"

<Meta title="Design/icon" />

# Iconography
##
<br />
<IconGallery>
{Object.entries(Icons)
.filter(([name])=> name !== "iconList" )
.map(([name,Icon])=>(

  <IconItem name={name}>
    <Icon />
  </IconItem>
))}
  
</IconGallery>
